{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch index.html",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "${workspaceFolder}/index.html",
      "webRoot": "${workspaceFolder}",
      "runtimeExecutable": "/path/to/chrome.exe" // Опционально, если хочешь задать точный путь к Chrome
    }
  ]
}